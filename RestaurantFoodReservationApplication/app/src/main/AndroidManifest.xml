<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurantfoodreservationapplication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantFoodReservationApplication" >
        <activity
            android:name=".QuanLyNhanVienActivity"
            android:label="@string/title_activity_quanly_nhanvien"
            android:theme="@style/Theme.RestaurantFoodReservationApplication.NoActionBar" >
        </activity>
        <activity android:name=".ThanhToanActivity" />
        <activity
            android:name=".DatMonActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.RestaurantFoodReservationApplication.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>